# Requirements
 - npm (or another package manager)

# How to get started
 - `npm install`  (adds requirements to repo)
 - `npm run start` or `npm run dev` to start the server. When using `dev` the server is restarted by file change